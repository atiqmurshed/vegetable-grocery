
const Heading = ({heading}) => {
    return (
        <div className="mt-5">
            <h1 className="text-semibold font-bold text-3xl ml-8">{heading}</h1>
        </div>
    );
};

export default Heading;